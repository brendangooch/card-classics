import { DynamicRectangle } from "@brendangooch/dynamic-objects";
export declare class ScreenEntity extends DynamicRectangle {
    static HIT_RADIUS_SCALE: number;
    private zIndex;
    private hitRadius;
    get z(): number;
    set z(zIndex: number);
    widthTo(width: number): number;
    heightTo(height: number): number;
    isHit(x: number, y: number): number | false;
    isHitPrecise(x: number, y: number): number | false;
    private updateHitRadius;
}
