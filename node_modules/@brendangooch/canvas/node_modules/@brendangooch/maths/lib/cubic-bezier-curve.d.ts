export declare class CubicBezierCurve {
    private s;
    private a;
    private b;
    private e;
    private equal;
    x(t: number): number;
    y(t: number): number;
    setStart(x: number, y: number): void;
    setControlA(x: number, y: number): void;
    setControlB(x: number, y: number): void;
    setEnd(x: number, y: number): void;
    setAll(x: number, y: number): void;
    save(): string;
    load(json: string): void;
    private calculateX;
    private calculateY;
    private checkEqual;
}
