import AbstractCanvasComponent from "./abstract-canvas-component.js";
import { tFontWeight } from "./index.js";
export default class CanvasFont extends AbstractCanvasComponent {
    private props;
    size(size: number): CanvasFont;
    family(family: string): CanvasFont;
    italic(): CanvasFont;
    bold(): CanvasFont;
    weight(weight: tFontWeight): CanvasFont;
    kerning(value: CanvasFontKerning): void;
    stretch(value: CanvasFontStretch): void;
    variantCaps(value: CanvasFontVariantCaps): void;
    reset(): void;
    private get sz();
    private get fm();
    private get st();
    private get wt();
    private update;
}
