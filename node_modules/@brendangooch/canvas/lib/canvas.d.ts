import CanvasColor from "./canvas-color.js";
import CanvasLine from './canvas-line.js';
import CanvasPath from "./canvas-path.js";
import CanvasFont from "./canvas-font.js";
import CanvasText from "./canvas-text.js";
import CanvasImage from "./canvas-image.js";
import CanvasFilter from "./canvas-filter.js";
import CanvasShadow from './canvas-shadow.js';
export declare class Canvas {
    static ALPHA_ENABLED: boolean;
    static SMOOTHING_ENABLED: boolean;
    static SMOOTHING_QUALITY: ImageSmoothingQuality;
    static ACCESSIBLE_CONTENT_ENABLED: boolean;
    ctx: CanvasRenderingContext2D;
    private canvas;
    private _color;
    private _line;
    private _path;
    private _font;
    private _text;
    private _image;
    private _filter;
    private _shadow;
    constructor(width: number, height: number, id?: string);
    get color(): CanvasColor;
    get line(): CanvasLine;
    get path(): CanvasPath;
    get font(): CanvasFont;
    get text(): CanvasText;
    get image(): CanvasImage;
    get filter(): CanvasFilter;
    get shadow(): CanvasShadow;
    clear(): void;
    save(fn: Function): void;
    translate(x: number, y: number): Canvas;
    rotate(radians: number): Canvas;
    opacity(alpha: number): Canvas;
    scale(x: number, y: number): Canvas;
    enableSmoothing(quality: ImageSmoothingQuality): void;
    disableSmoothing(): void;
    private addAccessibleContent;
}
