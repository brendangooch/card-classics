import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasText extends AbstractCanvasComponent {
    fill(text: string): CanvasText;
    stroke(text: string): CanvasText;
    align(value: CanvasTextAlign): CanvasText;
    baseline(value: CanvasTextBaseline): CanvasText;
    rendering(value: CanvasTextRendering): CanvasText;
    wordSpacing(amount: number): CanvasText;
    letterSpacing(amount: number): CanvasText;
    direction(value: CanvasDirection): CanvasText;
    measure(text: string): TextMetrics;
}
