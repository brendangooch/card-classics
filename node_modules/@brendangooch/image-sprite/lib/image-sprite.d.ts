import { iSprite } from "./index.js";
export declare class ImageSprite implements iSprite {
    private img;
    private numCols;
    private numRows;
    private current;
    constructor(img: HTMLImageElement, numCols: number, numRows: number);
    get image(): HTMLImageElement;
    get x(): number;
    get y(): number;
    get width(): number;
    get height(): number;
    column(column: number): ImageSprite;
    row(row: number): ImageSprite;
}
