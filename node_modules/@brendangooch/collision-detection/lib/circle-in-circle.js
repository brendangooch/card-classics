import { distanceBetween } from "@brendangooch/maths";
export function circleInCircle(c1x, c1y, c1radius, c2x, c2y, c2radius) {
    return distanceBetween(c1x, c1y, c2x, c2y) <= c1radius + c2radius;
}
