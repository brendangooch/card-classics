import { Vector2D } from "./vector-2d.js";
export declare class CubicBezierCurve {
    private start;
    private controlA;
    private controlB;
    private end;
    constructor(start: Vector2D, controlA: Vector2D, controlB: Vector2D, end: Vector2D);
    save(): string;
    load(json: string): void;
    x(t: number): number;
    y(t: number): number;
}
