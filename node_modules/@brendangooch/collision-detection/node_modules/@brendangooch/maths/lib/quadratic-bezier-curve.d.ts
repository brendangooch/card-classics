import { Vector2D } from "./vector-2d.js";
export declare class QuadraticBezierCurve {
    private start;
    private control;
    private end;
    constructor(start: Vector2D, control: Vector2D, end: Vector2D);
    save(): string;
    load(json: string): void;
    x(t: number): number;
    y(t: number): number;
}
