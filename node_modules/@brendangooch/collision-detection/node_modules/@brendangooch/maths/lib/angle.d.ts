export declare class Angle {
    private angle;
    constructor();
    init(turns: number): Angle;
    get turns(): number;
    get degrees(): number;
    get radians(): number;
    set turns(turns: number);
    set degrees(degrees: number);
    set radians(radians: number);
}
