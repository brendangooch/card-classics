import { tEaseOption } from "@brendangooch/ease";
import { iDynamic } from "./index.js";
export declare class DynamicPosition implements iDynamic {
    private bezier;
    private vector;
    private strategy;
    private currentStrategy;
    constructor(x?: number, y?: number);
    get isActive(): boolean;
    get x(): number;
    get y(): number;
    duration(ms: number): DynamicPosition;
    speed(unitsPerMs: number): DynamicPosition;
    ease(easeOption: tEaseOption): DynamicPosition;
    moveTo(x: number, y: number): number;
    curveTo(x: number, y: number, distance: number, angle: number): number;
    update(ms: number): void;
    load(json: string): boolean;
    save(): string;
    protected get isVector(): boolean;
    protected switchStrategy(): void;
    protected loadVector(): void;
    protected loadBezier(): void;
}
