import { tEaseOption } from "@brendangooch/ease";
import { iDynamic, tCurrentValue } from "./index.js";
export declare abstract class BaseDynamicObject implements iDynamic {
    protected _duration: number;
    protected easeOption: tEaseOption;
    protected isOn: boolean;
    abstract get isActive(): boolean;
    abstract get current(): tCurrentValue;
    duration(ms: number): void;
    ease(easeOption: tEaseOption): void;
    update(ms: number): void;
    save(): string;
    load(json: string): boolean;
    protected turnOn(): void;
    protected turnOff(): void;
    protected abstract increment(ms: number): void;
    protected abstract updateCurrent(): void;
    protected abstract updateComplete(): void;
}
