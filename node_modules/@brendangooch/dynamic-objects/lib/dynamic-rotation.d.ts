import { tEaseOption } from "@brendangooch/ease";
import { BaseDynamicObject } from "./base-dynamic-object.js";
export declare class DynamicRotation extends BaseDynamicObject {
    private static TAU;
    private rotation;
    private spin;
    private _speed;
    constructor(initial?: number);
    get isActive(): boolean;
    get current(): number;
    duration(ms: number): DynamicRotation;
    ease(easeOption: tEaseOption): DynamicRotation;
    speed(unitsPerMs: number): DynamicRotation;
    rotateTo(radians: number): number;
    spinTo(numSpins: number, radians: number): number;
    load(json: string): boolean;
    save(): string;
    protected increment(ms: number): void;
    protected updateCurrent(): void;
    protected updateComplete(): void;
    private canChange;
    private doChange;
    private instantChange;
    private dynamicChange;
    private reset;
    private addSpin;
    private removeSpin;
}
