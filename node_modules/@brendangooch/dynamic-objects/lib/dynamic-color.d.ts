import { tEaseOption } from "@brendangooch/ease";
import { BaseDynamicObject } from "./base-dynamic-object.js";
export declare class DynamicColor extends BaseDynamicObject {
    private r;
    private g;
    private b;
    private currentValue;
    constructor(r: number, g: number, b: number);
    get isActive(): boolean;
    get current(): string;
    duration(ms: number): DynamicColor;
    ease(easeOption: tEaseOption): DynamicColor;
    changeTo(r: number, g: number, b: number): number;
    load(json: string): boolean;
    save(): string;
    protected increment(ms: number): void;
    protected updateCurrent(): void;
    protected updateComplete(): void;
    private canChange;
    private doChange;
    private instantChange;
    private dynamicChange;
    private reset;
    private get red();
    private get green();
    private get blue();
}
