import * as Ease from '@brendangooch/ease';
import { BaseDynamicObject } from './base-dynamic-object.js';
import { iDynamicUnit } from './index.js';
export declare class DynamicUnit extends BaseDynamicObject implements iDynamicUnit {
    private elapsed;
    private currentValue;
    private easeFn;
    get isActive(): boolean;
    get current(): number;
    duration(ms: number): DynamicUnit;
    ease(easeOption: Ease.tEaseOption): DynamicUnit;
    save(): string;
    load(json: string): boolean;
    run(): boolean;
    clone(): DynamicUnit;
    protected increment(ms: number): void;
    protected updateCurrent(): void;
    protected updateComplete(): void;
    private get progress();
    private loadEase;
    private reset;
}
