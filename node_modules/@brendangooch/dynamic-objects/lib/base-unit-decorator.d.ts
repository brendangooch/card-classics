import * as Ease from '@brendangooch/ease';
import { DynamicUnit } from "./dynamic-unit.js";
import { iDynamicUnit } from "./index.js";
export declare abstract class BaseUnitDecorator implements iDynamicUnit {
    protected unit: DynamicUnit;
    constructor(unit: DynamicUnit);
    get isActive(): boolean;
    get current(): number;
    duration(ms: number): DynamicUnit;
    ease(easeOption: Ease.tEaseOption): DynamicUnit;
    run(): boolean;
    update(ms: number): void;
    load(json: string): boolean;
    save(): string;
    abstract clone(): iDynamicUnit;
}
