import { tEaseOption } from "@brendangooch/ease";
import { DynamicVector } from "./dynamic-vector.js";
export declare class DynamicQuadraticBezier extends DynamicVector {
    private bezier;
    private controlDistance;
    private controlAngle;
    duration(ms: number): DynamicQuadraticBezier;
    speed(unitsPerMs: number): DynamicQuadraticBezier;
    ease(easeOption: tEaseOption): DynamicQuadraticBezier;
    controlPoint(distance: number, angle: number): DynamicQuadraticBezier;
    save(): string;
    load(json: string): boolean;
    protected updateCurrent(): void;
    protected preDynamicChangeHook(): void;
    protected reset(): void;
}
