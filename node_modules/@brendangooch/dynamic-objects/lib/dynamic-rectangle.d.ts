import { tEaseOption } from "@brendangooch/ease";
import { tDynamicRectangleCurrentValue } from "./index.js";
import { BaseDynamicObject } from "./base-dynamic-object.js";
export declare class DynamicRectangle extends BaseDynamicObject {
    static DEFAULT_OPACITY: number;
    static DEFAULT_SCALE: number;
    static DEFAULT_WIDTH: number;
    static DEFAULT_HEIGHT: number;
    static MIN_OPACITY: number;
    static MIN_SCALE: number;
    static MIN_WIDTH: number;
    static MIN_HEIGHT: number;
    static MAX_OPACITY: number;
    static MAX_SCALE: number;
    static MAX_WIDTH: number;
    static MAX_HEIGHT: number;
    private components;
    private currentValues;
    private _speed;
    private visible;
    get isActive(): boolean;
    get current(): tDynamicRectangleCurrentValue;
    get isVisible(): boolean;
    duration(ms: number): DynamicRectangle;
    speed(unitsPerMs: number): DynamicRectangle;
    ease(easeOption: tEaseOption): DynamicRectangle;
    moveTo(x: number, y: number): number;
    curveTo(x: number, y: number, distance: number, angle: number): number;
    rotateTo(radians: number): number;
    spinTo(numSpins: number, radians: number): number;
    fadeTo(opacity: number): number;
    scaleTo(scale: number): number;
    widthTo(width: number): number;
    heightTo(height: number): number;
    show(): void;
    hide(): void;
    load(json: string): boolean;
    save(): string;
    protected increment(ms: number): void;
    protected updateCurrent(): void;
    protected updateComplete(): void;
    private updateCurrentPosition;
    private updateCurrentRotation;
    private updateCurrentOpacity;
    private updateCurrentScale;
    private updateCurrentWidth;
    private updateCurrentHeight;
    private get isDynamic();
    private dynamicChange;
    private setDuration;
    private setSpeed;
    private setEase;
    private reset;
}
