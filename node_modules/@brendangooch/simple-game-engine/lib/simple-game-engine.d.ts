import { Canvas } from "@brendangooch/canvas";
import { iUpdateable } from "@brendangooch/simple-game-loop";
import { iRenderable } from "@brendangooch/simple-renderer";
export declare class SimpleGameEngine {
    private gameLoop;
    private renderer;
    private running;
    constructor(canvas: Canvas);
    loop(objects: iUpdateable[]): void;
    render(objects: iRenderable[]): void;
    start(): void;
    stop(): void;
    toggle(): void;
}
