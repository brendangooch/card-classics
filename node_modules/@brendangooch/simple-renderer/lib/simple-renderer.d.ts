import { Canvas } from "@brendangooch/canvas";
import { iRenderable } from "./index.js";
export declare class SimpleRenderer {
    static FPS: number;
    private canvas;
    private running;
    private objectsToRender;
    private intervalID;
    constructor(canvas: Canvas);
    add(obj: iRenderable): void;
    start(): void;
    stop(): void;
    render(): void;
    private loadInterval;
}
