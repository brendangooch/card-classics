import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasLine extends AbstractCanvasComponent {
    width(n: number): CanvasLine;
    dash(segments: number[], offset?: number): CanvasLine;
    cap(type: CanvasLineCap): CanvasLine;
    join(type: CanvasLineJoin): CanvasLine;
}
