import AbstractCanvasComponent from "./abstract-canvas-component.js";
import type { tFontWeight } from "./index.js";
export default class CanvasFont extends AbstractCanvasComponent {
    private props;
    size(size: number): CanvasFont;
    family(family: string): CanvasFont;
    italic(): CanvasFont;
    bold(): CanvasFont;
    weight(weight: tFontWeight): CanvasFont;
    kerning(value: CanvasFontKerning): CanvasFont;
    stretch(value: CanvasFontStretch): CanvasFont;
    variantCaps(value: CanvasFontVariantCaps): CanvasFont;
    reset(): void;
    private get sz();
    private get fm();
    private get st();
    private get wt();
    private update;
}
