import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasPath extends AbstractCanvasComponent {
    fill(callback) {
        this.ctx.beginPath();
        callback();
        this.ctx.fill();
    }
    stroke(callback) {
        this.ctx.beginPath();
        callback();
        this.ctx.stroke();
    }
    clip(callback) {
        this.ctx.beginPath();
        callback();
        this.ctx.clip();
    }
    line(x1, y1, x2, y2) {
        this.ctx.moveTo(x1, y1);
        this.ctx.lineTo(x2, y2);
    }
    rectangle(width, height, radii = 0) {
        if (this.ctx.roundRect && radii)
            this.ctx.roundRect(-width / 2, -height / 2, width, height, radii);
        else
            this.ctx.rect(-width / 2, -height / 2, width, height);
    }
    circle(radius) {
        this.ctx.arc(0, 0, radius, 0, Math.PI * 2, false);
    }
    square(length, radii = 0) {
        this.rectangle(length, length, radii);
    }
}
