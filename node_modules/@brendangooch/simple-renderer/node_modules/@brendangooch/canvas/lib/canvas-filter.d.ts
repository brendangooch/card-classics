import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasFilter extends AbstractCanvasComponent {
    private filters;
    blur(n: number): CanvasFilter;
    brightness(n: number): CanvasFilter;
    contrast(n: number): CanvasFilter;
    grayscale(n: number): CanvasFilter;
    hueRotate(n: number): CanvasFilter;
    invert(n: number): CanvasFilter;
    opacity(n: number): CanvasFilter;
    saturate(n: number): CanvasFilter;
    sepia(n: number): CanvasFilter;
    reset(): void;
    protected update(): void;
}
