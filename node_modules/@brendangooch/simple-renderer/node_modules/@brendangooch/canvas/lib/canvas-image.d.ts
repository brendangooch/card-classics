import type { iSprite } from "@brendangooch/image-sprite";
import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasImage extends AbstractCanvasComponent {
    original(image: HTMLImageElement): void;
    scaled(image: HTMLImageElement, width: number, height: number): void;
    segment(image: HTMLImageElement, sx: number, sy: number, sWidth: number, sHeight: number, dWidth: number, dHeight: number): void;
    sprite(sprite: iSprite, width: number, height: number): void;
}
