import AbstractCanvasComponent from "./abstract-canvas-component.js";
export default class CanvasShadow extends AbstractCanvasComponent {
    private props;
    x(n: number): CanvasShadow;
    y(n: number): CanvasShadow;
    blur(n: number): CanvasShadow;
    color(c: string): CanvasShadow;
    reset(): void;
    private update;
}
