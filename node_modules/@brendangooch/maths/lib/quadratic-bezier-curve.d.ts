export declare class QuadraticBezierCurve {
    private s;
    private c;
    private e;
    private equal;
    x(t: number): number;
    y(t: number): number;
    setStart(x: number, y: number): QuadraticBezierCurve;
    setControl(x: number, y: number): QuadraticBezierCurve;
    setEnd(x: number, y: number): QuadraticBezierCurve;
    setAll(x: number, y: number): void;
    distanceBetween(): number;
    setControlByDistanceAndAngleFromStart(distance: number, angle: number): void;
    save(): string;
    load(json: string): void;
    private calculateX;
    private calculateY;
    private checkEqual;
}
